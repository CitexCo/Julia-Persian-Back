include ../header.pug 
div
     p سلام،
     p درخواست شماره #{transferRequestNumber} جهت انتقال #{amount} توکن توسط مدیر سیستم 
          if approved
               | تایید شد.
          else
               | رد شد.
     include ../signature.pug 